# Copy to `.env` and fill in. `.env` is gitignored — never commit real secrets.
# For GitHub Actions, these same keys go in the repo's Actions secrets instead.

# --- Notion (read-only) ---
# Create an internal integration at https://www.notion.so/my-integrations,
# copy its token here, then share BOTH databases with the integration.
NOTION_TOKEN=ntn_xxx

# Database IDs are auto-discovered by the introspect script, but once known you
# can pin them here to skip discovery (optional).
INGREDIENTS_DB_ID=
MEAL_PREP_DB_ID=

# Which meal plan is currently active (matched against the plan row's name).
ACTIVE_PLAN=Plan 1.8k

# --- Telegram (alerts) ---
# Create a bot via @BotFather, paste its token; get your chat id from the bot.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
